/**
 * @format
 */

import { AppRegistry } from 'react-native';
import Selection from './screens/selection/Selection';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => Selection);
